version: '3.8'

services:
  app:
    build: ./app
    ports:
      - "8000:8000"
    volumes:
      - ./app/models:/app/models:ro
    restart: unless-stopped

  gradio_ui:
    build: ./gradio_ui
    ports:
      - "7860:7860"
    depends_on:
      - app
    restart: unless-stopped